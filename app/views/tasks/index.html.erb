<h1>List of tasks</h1>

  <% @tasks.each do |task| %>
        <form action="/action_page.php">
          <input type="checkbox" name="completed" value="check" <%=  task.completed ? "checked" : "" %> >
            <%= link_to task.title, task_path(task) %>
          <br>
        </form>
  <% end %>
